package cn.practice.Others._01_BasicJavaMethod.BZhan._02_JDK8.lambda.practice;

/**
 * Lambda表达式是Java SE 8中一个重要的新特性。
 * lambda表达式允许你通过表达式来代替功能接口。
 * lambda表达式就和方法一样,它提供了一个正常的参数列表和一个使用这些参数
 * 的主体(body,可以是一个表达式或一个代码块)。
 * Lambda表达式还增强了集合库。
 * Java SE 8添加了2个对集合数据进行批量操作的包: 
 * java.util.function 包以及java.util.stream 包。
 * 流(stream)就如同迭代器(iterator),但附加了许多额外的功能。
 * 总的来说,lambda表达式和 stream 是自Java语言添加泛型(Generics)
 * 和注解(annotation)以来最大的变化。
 * 在本文中,我们将从简单到复杂的示例中见认识lambda表达式和stream的强悍。
 */
public class Test {
}
